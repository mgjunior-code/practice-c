#include <iostream>

int main() {
  
  double tempf;
  double tempc;
  
  // Ask the user
  
  
  
  // tempc = (tempf - 32) / 1.8;
  
  std::cout << "Enter the temperature in Fahrenheit /n";

  std::cin >> tempf;

  tempc = (tempf - 32) / 1.8;

    std::cout << tempc;
  
}
